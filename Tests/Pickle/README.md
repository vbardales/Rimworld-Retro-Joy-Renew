# Pickle suite

Minimal, no optional-mod pass. Run the companion named `Retro Joy Renew - Pickle tests` through `Run-PickleWsl.ps1`; use separate English and French passes. The second scenario is intentionally review-only until a shared built-in placement step is confirmed for this fixture.
